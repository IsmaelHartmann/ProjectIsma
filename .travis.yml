language: php
php:
  - '7.4'

before_script:
  - composer self-update
  - composer install --no-interaction
  - cp .env.travis .env
script:
  - vendor/bin/phpunit
deploy:
  provider: heroku
  app: quiet-coast-59009
  api_key: d99f6dde-b572-4a61-b3f2-ff629203c4f3
  run: php artisan migrate --seed --force
  on:
    branch: master

